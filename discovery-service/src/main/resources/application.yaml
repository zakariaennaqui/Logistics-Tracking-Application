spring:
  application:
    name: discovery-service

server:
  port: ${PORT:7860}

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false #Useful in development, not recommended in production.
    eviction-interval-timer-in-ms: 15000
